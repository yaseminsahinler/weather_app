name: weather_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.1

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.3.0
  dio: ^5.4.0
  flutter_dotenv: ^5.1.0
  intl: ^0.20.3
  geolocator: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/weatherbackground.svg
    - assets/weather_icon.svg 
    - assets/weather_icon_2.svg
    - .env